version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: utg_allscore_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
